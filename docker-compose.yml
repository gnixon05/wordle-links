services:
  app:
    build: .
    ports:
      - "80:3001"
      - "3001:3001"
    environment:
      - NODE_ENV=production
      - PORT=3001
      - DATABASE_PATH=/app/data/word-tour.db
    volumes:
      - db-data:/app/data
    restart: unless-stopped

volumes:
  db-data:
